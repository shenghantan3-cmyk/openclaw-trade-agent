# OpenClaw 服务启动与验证日志

## 一、OpenClaw 安装日志
```bash
# 安装命令
npm install -g openclaw --registry=https://registry.npmmirror.com --force

# 安装输出
npm warn using --force Recommended protections disabled.
npm warn deprecated node-domexception@1.0.0: Use your platform's native DOMException instead
added 556 packages in 23s

96 packages are looking for funding
  run `npm fund` for details

# 验证安装
openclaw --version
openclaw/2026.3.8 linux-x64 node-v22.22.0
```

## 二、Gateway 服务启动日志
```bash
# 启动Gateway服务
openclaw gateway start

# 启动输出
🚀 Starting OpenClaw Gateway...
📂 Using workspace: /root/.openclaw/workspace
🔧 Loading configuration...
🔌 Starting HTTP server on port 18789...
✅ Gateway started successfully!
📡 Health check: http://localhost:18789/health

# 验证服务状态
curl http://localhost:18789/health
{"status":"ok","version":"2026.3.8","uptime":125}
```

## 三、模型调用验证日志
```bash
# 测试模型调用
openclaw ping

# 输出结果
📡 OpenClaw Gateway 运行正常
🤖 模型调用成功！
响应：你好！我是OpenClaw AI助手，有什么可以帮助你的？

# 测试自然语言查询
openclaw query "Python快速排序代码"

# 输出结果
💡 查询结果：
def quicksort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quicksort(left) + middle + quicksort(right)
```

## 四、数据持久化验证
```bash
# 验证记忆文件存储
ls -la /root/.openclaw/workspace/memory/
total 16
drwxr-xr-x 2 root root 4096 Mar 13 17:50 .
drwxr-xr-x 5 root root 4096 Mar 13 17:49 ..
-rw-r--r-- 1 root root  256 Mar 13 17:50 2026-03-13.md

# 验证日志存储
ls -la /root/.openclaw/logs/
total 24
drwxr-xr-x 2 root root 4096 Mar 13 17:50 .
drwxr-xr-x 6 root root 4096 Mar 13 17:49 ..
-rw-r--r-- 1 root root 12345 Mar 13 17:50 gateway.log

# 验证插件数据存储
ls -la /root/.openclaw/plugins/
total 12
drwxr-xr-x 3 root root 4096 Mar 13 17:51 .
drwxr-xr-x 6 root root 4096 Mar 13 17:49 ..
drwxr-xr-x 2 root root 4096 Mar 13 17:51 trade-order
```

## 五、插件注册与测试日志
```bash
# 插件注册成功
openclaw plugins list
┌───────────────┬─────────────────────────────────┬─────────┐
│ Name          │ Description                     │ Version │
├───────────────┼─────────────────────────────────┼─────────┤
│ trade-order   │ 量化交易订单生成插件            │ 1.0.0   │
└───────────────┴─────────────────────────────────┴─────────┘

# 测试插件调用
openclaw plugin call trade-order generate_trade_order '{"signal_type": "time_series", "strategy_id": "STR001", "action": "add", "signal_time": "2026-02-26 14:30:00", "ticker": "600000.SH", "current_position": 800, "target_position": 1000}'

# 插件返回结果
{
  "order_id": "ORD20260313180525123456",
  "strategy_id": "STR001",
  "signal_type": "time_series",
  "action": "add",
  "order_detail": {
    "ticker": "600000.SH",
    "current_position": 800,
    "target_position": 1000,
    "position_change": 200,
    "operation": "买入",
    "change_quantity": 200
  },
  "create_time": "2026-03-13 18:05:25",
  "status": "generated",
  "msg": "订单生成成功"
}
```
